<%= form_tag("/search", method: "get") do %>
    <%= label_tag(:student_name, "Search for:") %>
    <%= text_field_tag(:name) %>
    <%= submit_tag("Search") %>
  <% end %>

  
    <%= render partial: "student", locals: {student: Student.search(:name).first } %>
  
  